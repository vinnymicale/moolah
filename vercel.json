{
  "framework": "nextjs",
  "buildCommand": "[ \"$VERCEL_ENV\" != \"production\" ] || prisma migrate deploy && prisma generate && next build",
  "installCommand": "npm install",
  "regions": ["iad1"]
}
